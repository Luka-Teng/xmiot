module.exports = {
  rules: {
    'type-case': [2, 'always', 'lower-case'],
    'type-empty': [1, 'never'],
    'type-enum': [
      2,
      'always',
      ['chore', 'module', 'bugfix', 'refactor', 'Publish']
    ]
  }
}
